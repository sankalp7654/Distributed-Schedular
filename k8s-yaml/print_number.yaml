apiVersion: apps/v1
kind: Deployment
metadata:
  name: print-number-deployment-BEGIN_VALUE
  labels:
    app: print-number
spec:
  replicas: 1
  selector:
    matchLabels:
      app: print-number
  template:
    metadata:
      labels:
        app: print-number
    spec:
      containers:
      - name: print-number
        image: sankalpsaxena/print_number:latest
        ports:
        - containerPort: 80
        env:
        - name: START_INDEX
          value: "BEGIN_VALUE"
        - name: END_INDEX
          value: "LAST_VALUE"
